import React, { useState } from 'react';

export default function SearchBar() {
  
  
  return (
    <form> 
        <input type="text"/>
        <button type= "submit"> Submit </button>
      </form>
  );
};